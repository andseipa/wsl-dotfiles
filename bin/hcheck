# /bin/bash
#
# Temporary bash script for determine hostname for NOTESDIR

hostcheck=`hostname`

h1=andreass
h2=andreasb

if [ "$hostcheck" = "$h1" ]
	then
	export NOTESDIR="/mnt/d/Dropbox/Notes/"
	else
	export NOTESDIR="/mnt/c/Dropbox/Notes/"
fi
